name: Deploy to gh-pages

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Simple deploy with git
        uses: rdarida/simple-github-pages-deploy-action@v
        with: # optional
          git-user: 'FebriPutraTrisBudiono'
          git-email: 'febript@gmail.com'
          git-base-folder: '.'
          commit-message: 'new text'
          branch: 'gh-pages'
